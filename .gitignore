# credenciais — nunca commitar
.env
.env.local

# python
__pycache__/
*.pyc

# artefatos temporarios
*.log
~$*
.playwright-mcp/

# relatorio gerado — a fonte e test/releases/*.md, o .docx e derivado
test/Relatorio_de_Testes_*.docx

# painel gerado — derivado de test/cases, test/runs e bugs/.
# O snapshot em test/metricas/ E versionado: e ele que cria a serie historica.
test/dashboard.html

# saida de runner, antes do qa_ingest.py
resultados.xml
junit*.xml

# evidencia pesada: decida a politica de retencao antes de versionar video
*.webm
*.zip
